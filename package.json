
{
  "name": "teamdrive-mobile-hub",
  "version": "1.0.0",
  "private": true,
  "description": "A collaborative mobile-first application for teams to store, share, and analyze files.",
  "main": "index.js",
  "scripts": {
    "dev": "echo \"Development server not needed for this static setup. Just serve index.html\" && exit 1",
    "build": "echo \"No build step required for this project.\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react",
    "gemini",
    "vercel",
    "files"
  ],
  "author": "AI Assistant",
  "license": "UNLICENSED",
  "dependencies": {
    "@google/genai": "^1.9.0",
    "@vercel/node": "^3.2.0"
  }
}
